@model FlowerInventory.Models.Flower

<div class="container my-5">
    <div class="card shadow-lg border-0 rounded-3">
        <div class="card-header bg-primary text-white">
            <h2 class="mb-0">
                🌸 @Model.Name
            </h2>
        </div>
        <div class="card-body">
            <p class="fs-5"><strong>Category:</strong> @Model.Category?.Name</p>
            <p class="fs-5"><strong>Price:</strong> €@Model.Price.ToString("0.00")</p>
        </div>
        <div class="card-footer d-flex justify-content-between">
            <a href="@Url.Action("Index")" class="btn btn-secondary">
                <i class="bi bi-arrow-left"></i> Back to List
            </a>
            <div>
                <a href="@Url.Action("Upsert", new { id = Model.Id })" class="btn btn-warning">
                    <i class="bi bi-pencil-square"></i> Edit
                </a>
                <a href="@Url.Action("Delete", new { id = Model.Id })" class="btn btn-danger">
                    <i class="bi bi-trash"></i> Delete
                </a>
            </div>
        </div>
    </div>
</div>
